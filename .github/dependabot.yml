version: 2

updates:
- package-ecosystem: "pip"
  directory: "/"

  schedule:
    interval: "weekly"

  open-pull-requests-limit: 1

  labels:
  - dependencies

  commit-message:
    prefix: "deps"

  groups:
    python-dependencies:
      patterns:
      - "*"
